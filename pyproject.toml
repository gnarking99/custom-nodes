[project]
name = "comfyui-character-pipeline"
description = "Nodos para flujos de recuperacion de detalle de personajes con LoRA (Flux.2 Klein / LTX-2.3 / Wan 2.2)."
version = "0.1.0"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/gnarking99/custom-nodes"

[tool.comfy]
PublisherId = "gnarking99"
DisplayName = "Character Pipeline"
